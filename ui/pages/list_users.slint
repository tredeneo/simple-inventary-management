
import { AboutSlint,StandardTableView, VerticalBox } from "std-widgets.slint";


export global Users {
    in property <[[StandardListViewItem]]> row_data : [
            [{text:"daniel.inacio"},{text:"ti"}],
            [{text:"gustavo.bustos"},{text:"comprovante"}],
            [{text:"aliane.santos"},{text:"qualidade"}],
            ];
}

export component UserList inherits VerticalBox {
    StandardTableView {
        columns:[
                {title: "usuario"},
                {title: "setor"}
            ];
        rows: Users.row-data;
    

    }       
}   
