

export global GlobalRole{
    in-out property <[StandardListViewItem]> row_data:[
        {text:"Assistente de TI"},{text:"Analista Comercial"},{text:"Supervisor de Filial"}
    ] ;    
    callback delete_item(StandardListViewItem);
    callback add_item(string);
}
